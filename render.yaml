services:
  - type: static
    name: thoughtflow-media-hub
    env: static
    plan: free
    buildCommand: npm install && npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
